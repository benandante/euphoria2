// File: readConfig.js


// Start function when DOM has completely loaded 

var userName = "aa";
var userSurname = "";
var userEmail = "";

/*$(document).ready(function(){ 		
	// Open the users.xml file
	$.get("config/users.xml",{},function(xml){
		// Run the function for each user tag in the XML file
		$('user', xml).each(function(i) {
			userName = $(this).find("name").text();
			userSurname = $(this).find("surname").text();
			userEmail = $(this).find("email").text();
		});
	});
	
});*/
/**/